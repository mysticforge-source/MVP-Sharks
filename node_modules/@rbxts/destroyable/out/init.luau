-- Compiled with roblox-ts v3.0.0
local TS = _G[script]
local Trash = TS.import(script, TS.getModule(script, "@rbxts", "trash").out).Trash
local Destroyable
do
	Destroyable = setmetatable({}, {
		__tostring = function()
			return "Destroyable"
		end,
	})
	Destroyable.__index = Destroyable
	function Destroyable.new(...)
		local self = setmetatable({}, Destroyable)
		return self:constructor(...) or self
	end
	function Destroyable:constructor()
		self.trash = Trash.new()
	end
	function Destroyable:destroy()
		Trash:tryDestroy(self.trash)
	end
end
return Destroyable
