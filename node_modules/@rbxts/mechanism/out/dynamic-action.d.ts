import { BaseStandardAction } from "./standard-action";
export declare class DynamicAction extends BaseStandardAction {
    readonly action: BaseStandardAction;
    private readonly connections;
    constructor(action: BaseStandardAction);
    updateAction(action: BaseStandardAction): void;
    /** @hidden */
    handleInput(input: InputObject, processed: boolean): void;
}
