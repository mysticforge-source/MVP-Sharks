/* aspect ratio constraint component */

import Vide from "@rbxts/vide";

export const Aspect = ({ aspectRatio }: { aspectRatio: number }) => (
	<uiaspectratioconstraint AspectRatio={aspectRatio} />
);
